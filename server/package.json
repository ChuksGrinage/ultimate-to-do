{
  "name": "server",
  "module": "src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "gen-env-types": "bunx gen-env-types .env -o src/types/env.d.ts -e ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.6",
    "drizzle-kit": "^0.24.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "pg": "^8.12.0"
  }
}
